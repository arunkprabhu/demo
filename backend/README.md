# Backend (Express)

Simple Express backend.

## Install

```bash
cd backend
npm install
```

## Run (development)

```bash
npm run dev
```

Default port: `5000` (or set `PORT` env var)

## Endpoints

- GET `/api` — returns `{ "message": "hello" }`
